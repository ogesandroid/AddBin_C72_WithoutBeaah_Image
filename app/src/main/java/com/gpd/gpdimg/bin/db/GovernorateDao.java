package com.gpd.gpdimg.bin.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table "GOVERNORATE".
*/
public class GovernorateDao extends AbstractDao<Governorate, Long> {

    public static final String TABLENAME = "GOVERNORATE";

    /**
     * Properties of entity Governorate.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Status = new Property(1, String.class, "status", false, "STATUS");
        public final static Property Governorate_name = new Property(2, String.class, "governorate_name", false, "GOVERNORATE_NAME");
        public final static Property Governorate_value = new Property(3, String.class, "governorate_value", false, "GOVERNORATE_VALUE");
        public final static Property Governorate_id = new Property(4, String.class, "governorate_id", false, "GOVERNORATE_ID");
    }


    public GovernorateDao(DaoConfig config) {
        super(config);
    }
    
    public GovernorateDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"GOVERNORATE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"STATUS\" TEXT," + // 1: status
                "\"GOVERNORATE_NAME\" TEXT," + // 2: governorate_name
                "\"GOVERNORATE_VALUE\" TEXT," + // 3: governorate_value
                "\"GOVERNORATE_ID\" TEXT);"); // 4: governorate_id
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"GOVERNORATE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Governorate entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String status = entity.getStatus();
        if (status != null) {
            stmt.bindString(2, status);
        }
 
        String governorate_name = entity.getGovernorate_name();
        if (governorate_name != null) {
            stmt.bindString(3, governorate_name);
        }
 
        String governorate_value = entity.getGovernorate_value();
        if (governorate_value != null) {
            stmt.bindString(4, governorate_value);
        }
 
        String governorate_id = entity.getGovernorate_id();
        if (governorate_id != null) {
            stmt.bindString(5, governorate_id);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Governorate entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String status = entity.getStatus();
        if (status != null) {
            stmt.bindString(2, status);
        }
 
        String governorate_name = entity.getGovernorate_name();
        if (governorate_name != null) {
            stmt.bindString(3, governorate_name);
        }
 
        String governorate_value = entity.getGovernorate_value();
        if (governorate_value != null) {
            stmt.bindString(4, governorate_value);
        }
 
        String governorate_id = entity.getGovernorate_id();
        if (governorate_id != null) {
            stmt.bindString(5, governorate_id);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Governorate readEntity(Cursor cursor, int offset) {
        Governorate entity = new Governorate( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // status
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // governorate_name
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // governorate_value
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // governorate_id
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Governorate entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setStatus(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setGovernorate_name(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setGovernorate_value(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setGovernorate_id(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Governorate entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Governorate entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Governorate entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
