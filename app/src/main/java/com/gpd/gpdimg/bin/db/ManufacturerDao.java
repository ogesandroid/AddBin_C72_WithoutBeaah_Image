package com.gpd.gpdimg.bin.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table "MANUFACTURER".
*/
public class ManufacturerDao extends AbstractDao<Manufacturer, Long> {

    public static final String TABLENAME = "MANUFACTURER";

    /**
     * Properties of entity Manufacturer.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Status = new Property(1, String.class, "status", false, "STATUS");
        public final static Property Manufacturer_id = new Property(2, String.class, "manufacturer_id", false, "MANUFACTURER_ID");
        public final static Property Manufacturer_name = new Property(3, String.class, "manufacturer_name", false, "MANUFACTURER_NAME");
    }


    public ManufacturerDao(DaoConfig config) {
        super(config);
    }
    
    public ManufacturerDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"MANUFACTURER\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"STATUS\" TEXT," + // 1: status
                "\"MANUFACTURER_ID\" TEXT," + // 2: manufacturer_id
                "\"MANUFACTURER_NAME\" TEXT);"); // 3: manufacturer_name
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"MANUFACTURER\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Manufacturer entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String status = entity.getStatus();
        if (status != null) {
            stmt.bindString(2, status);
        }
 
        String manufacturer_id = entity.getManufacturer_id();
        if (manufacturer_id != null) {
            stmt.bindString(3, manufacturer_id);
        }
 
        String manufacturer_name = entity.getManufacturer_name();
        if (manufacturer_name != null) {
            stmt.bindString(4, manufacturer_name);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Manufacturer entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String status = entity.getStatus();
        if (status != null) {
            stmt.bindString(2, status);
        }
 
        String manufacturer_id = entity.getManufacturer_id();
        if (manufacturer_id != null) {
            stmt.bindString(3, manufacturer_id);
        }
 
        String manufacturer_name = entity.getManufacturer_name();
        if (manufacturer_name != null) {
            stmt.bindString(4, manufacturer_name);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Manufacturer readEntity(Cursor cursor, int offset) {
        Manufacturer entity = new Manufacturer( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // status
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // manufacturer_id
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // manufacturer_name
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Manufacturer entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setStatus(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setManufacturer_id(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setManufacturer_name(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Manufacturer entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Manufacturer entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Manufacturer entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
